import 'package:flutter/material.dart';

class WritingView extends StatelessWidget {
  const WritingView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('WritingPage'),
      ),
    );
  }
}
